---
import "styles/globals.css";

import AppLayout from "~/components/app/AppLayout";
import PortfolioEntry from "~/components/portfolio/PortfolioEntry";
import Head from "~/layouts/components/Head.astro";

const { title, description, content } = Astro.props;

export type Props = {
  title?: string;
  description?: string;
};
---

<!DOCTYPE html>
<html lang="en">
  <Head title={title} description={description} />
  <AppLayout currentPathName={Astro.url.pathname} client:visible>
    <PortfolioEntry {...content} client:visible>
      <slot />
    </PortfolioEntry>
  </AppLayout>
</html>
